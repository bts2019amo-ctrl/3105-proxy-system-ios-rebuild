name: Build iOS

on:
  workflow_dispatch:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-14
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Inspect toolchain
        run: |
          xcodebuild -version
          xcrun --sdk iphoneos --show-sdk-version

      - name: Detect project
        id: detect
        shell: bash
        run: |
          set -euo pipefail
          workspace=$(find . -maxdepth 3 -name '*.xcworkspace' -print -quit)
          project=$(find . -maxdepth 3 -name '*.xcodeproj' -print -quit)
          if [[ -n "$workspace" ]]; then
            echo "kind=workspace" >> "$GITHUB_OUTPUT"
            echo "path=$workspace" >> "$GITHUB_OUTPUT"
          elif [[ -n "$project" ]]; then
            echo "kind=project" >> "$GITHUB_OUTPUT"
            echo "path=$project" >> "$GITHUB_OUTPUT"
          else
            echo 'Nenhum .xcodeproj ou .xcworkspace encontrado.' >&2
            exit 1
          fi
      - name: List schemes
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${{ steps.detect.outputs.kind }}" == "workspace" ]]; then
            xcodebuild -list -workspace "${{ steps.detect.outputs.path }}"
          else
            xcodebuild -list -project "${{ steps.detect.outputs.path }}"
          fi

      - name: Build unsigned iOS app
        shell: bash
        run: |
          set -euo pipefail
          path="${{ steps.detect.outputs.path }}"
          scheme="3105"
          if [[ "${{ steps.detect.outputs.kind }}" == "workspace" ]]; then
            xcodebuild -workspace "$path" -scheme "$scheme" -configuration Release \
              -sdk iphoneos -destination 'generic/platform=iOS' \
              -derivedDataPath build CODE_SIGNING_ALLOWED=NO \
              CODE_SIGNING_REQUIRED=NO build
          else
            xcodebuild -project "$path" -scheme "$scheme" -configuration Release \
              -sdk iphoneos -destination 'generic/platform=iOS' \
              -derivedDataPath build CODE_SIGNING_ALLOWED=NO \
              CODE_SIGNING_REQUIRED=NO build
          fi

      - name: Package unsigned IPA
        if: success()
        shell: bash
        run: |
          set -euo pipefail
          app=$(find build/Build/Products/Release-iphoneos -maxdepth 1 -name '*.app' -print -quit)
          test -n "$app"
          mkdir -p ipa/Payload
          cp -R "$app" ipa/Payload/
          (cd ipa && zip -qry ../3105-unsigned.ipa Payload)

      - name: Upload unsigned IPA
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: 3105-unsigned-ipa
          path: 3105-unsigned.ipa
          if-no-files-found: error

      - name: Upload unsigned app bundle
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: 3105-unsigned-app
          path: build/Build/Products/Release-iphoneos/*.app
          if-no-files-found: error
